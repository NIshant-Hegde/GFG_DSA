def isPalindrome(str, start, end):
    if start>=end:
      return True
    return ((str[start]==str[end]) and isPalindrome(str, start + 1, end - 1))

st = "abc"
if isPalindrome(st, start=0, end=len(st)-1):
   print("Yes")
else:
   print("No")

